{
  "phases": {
    "build": {
      "cmds": ["mvn clean package -DskipTests"]
    },
    "start": {
      "cmd": "java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/f1-backend-0.0.1-SNAPSHOT.jar"
    }
  }
}
